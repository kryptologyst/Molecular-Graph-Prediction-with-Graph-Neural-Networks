# @package model
_target_: src.models.gin.GIN

# Model architecture
hidden_dim: 64
num_layers: 3
dropout: 0.1
use_batch_norm: true
use_residual: true

# GIN-specific settings
eps: 0.0
train_eps: true

# Task-specific settings
num_tasks: 12
task_type: multilabel_classification
